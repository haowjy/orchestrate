# Orchestrate Skill & Agent Manifest
# Items listed here are available for syncing via sync.sh.
# Lines starting with # are comments. Empty lines are ignored.
#
# Core skills (always installed):
orchestrate
run-agent

# Optional skills (sync via --skills <name> or --all):
review
research
smoke-test
scratchpad
plan-slice
mermaid
model-guidance

# Optional agents (sync via --agents <name> or --all):
coder
researcher
reviewer
