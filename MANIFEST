# Orchestrate Skill Manifest
# Skills listed here are available for installation via install.sh.
# Lines starting with # are comments. Empty lines are ignored.
#
# Core skills (always installed):
orchestrate
run-agent

# Available skills (install via --include <name> or --all):
review
research
smoke-test
scratchpad
plan-slice
mermaid
backlog
model-guidance
